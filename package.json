{
  "name": "illustrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "biome format --write .",
    "check": "biome check --write .",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean && rm -rf node_modules",
    "setApiEnv": "cp apps/api/.env.example apps/api/.env",
    "setWorkerEnv": "cp apps/worker/.env.example apps/worker/.env",
    "setWebEnv": "cp apps/web/.env.example apps/web/.env",
    "setEnv": "cp .env.example .env && npm run setApiEnv && npm run setWorkerEnv && npm run setWebEnv"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
